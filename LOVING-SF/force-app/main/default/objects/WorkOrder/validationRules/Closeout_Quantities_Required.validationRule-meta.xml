<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Closeout_Quantities_Required</fullName>
    <active>true</active>
    <description>Closeout_Quantities_Confirmed__c must be true before status can change to Pending Closeout Approval.</description>
    <errorConditionFormula>AND(
        ISPICKVAL(Status, "Pending Closeout Approval"),
        NOT(ISPICKVAL(PRIORVALUE(Status), "Pending Closeout Approval")),
        NOT(Closeout_Quantities_Confirmed__c)
    )</errorConditionFormula>
    <errorMessage>Quantities must be confirmed before closeout approval. Review each Line Item, confirm installed quantities, and check Closeout Quantities Confirmed. Note outside-PO scope in Outside PO Notes.</errorMessage>
</ValidationRule>
