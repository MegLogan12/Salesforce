<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>NFI_Notes_Required</fullName>
    <active>true</active>
    <description>Need_Further_Info_Notes__c must describe the specific issue when status is set to Need Further Info.</description>
    <errorConditionFormula>AND(
        ISPICKVAL(Status, "Need Further Info"),
        NOT(ISPICKVAL(PRIORVALUE(Status), "Need Further Info")),
        ISBLANK(Need_Further_Info_Notes__c)
    )</errorConditionFormula>
    <errorMessage>NFI Notes are required. Describe: (1) what information is missing, (2) what the FM must do to resolve it, and (3) whether photos are needed. This note is the FM's only instruction.</errorMessage>
</ValidationRule>
